import React from 'react';
import { expect } from 'chai';
import Showcasecarousel from './ShowcaseCarousel';

describe('Spec Showcasecarousel', function () {
    it('it exists', () => {
        expect(Showcasecarousel).to.be.ok;
    });
});

import React from 'react';
import { expect } from 'chai';
import Recentwork from './RecentWork';

describe('Spec Recentwork', function () {
    it('it exists', () => {
        expect(Recentwork).to.be.ok;
    });
});

import React from 'react';
import { expect } from 'chai';
import Horizontalcarousel from './HorizontalCarousel';

describe('Spec Horizontalcarousel', function () {
    it('it exists', () => {
        expect(Horizontalcarousel).to.be.ok;
    });
});

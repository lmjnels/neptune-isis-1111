import React from 'react';
import HorizontalCarousel, { HorizontalCarouselProps } from './HorizontalCarousel';

export default {
    title: "Horizontalcarousel",
    component: HorizontalCarousel
};

export const Default = (props: HorizontalCarouselProps) => <HorizontalCarousel {...props} />;

import React from 'react';
import { expect } from 'chai';
import Navigationbar from './NavigationBar';

describe('Spec Navigationbar', function () {
    it('it exists', () => {
        expect(Navigationbar).to.be.ok;
    });
});
